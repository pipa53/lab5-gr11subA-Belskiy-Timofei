# **1. Цель работы**
Целью данной лабораторной работы является изучение разработки модулей ядра Linux. В рамках работы были реализованы следующие задачи:
- Создание простого Hello World модуля.
- Создание файла `/proc/student_info` с информацией о студенте.
- Реализация простого символьного устройства `/dev/mychardev`.

---

# **2. Структура репозитория**
```
lab5/
└── gr11subA/
    └── Бельский_Тимофей/
        ├── REPORT.MD          # Отчёт
        ├── src/               # Исходные коды модулей
        │   ├── hello_module.c # Код Hello World модуля
        │   ├── proc_module.c  # Код модуля /proc/student_info
        │   ├── chardev_module.c # Код символьного устройства
        │   └── Makefile       # Makefile для сборки
```

---

# **3. Задания**

## **Задание A: Hello World модуль**

### **Описание**
В рамках этого задания был создан простой модуль ядра Linux, который:
1. При загрузке (`insmod`) выводит сообщение `"Hello from [Бельский Тимофей] module!"`.
2. При выгрузке (`rmmod`) выводит сообщение `"Goodbye from [Бельский Тимофей] module!"`.
3. Поддерживает передачу параметра `message` при загрузке. Если параметр задан, выводит его вместо дефолтного сообщения.

---

### **Код модуля**
```c
#include <linux/module.h>    // Обязательно для всех модулей
#include <linux/kernel.h>    // Для KERN_INFO, printk
#include <linux/init.h>      // Для макросов __init и __exit
#include <linux/moduleparam.h> // Для параметров модуля

// Параметр по умолчанию
static char *message = "default";
module_param(message, charp, 0644); // Регистрация параметра
MODULE_PARM_DESC(message, "Custom message to display");

// Функция инициализации (вызывается при insmod)
static int __init hello_init(void) {
    if (!message) { // Проверка на NULL
        printk(KERN_ERR "Error: Invalid or missing message parameter.\n");
        return -EINVAL; // Возвращаем ошибку
    }

    if (strcmp(message, "default") == 0) {
        printk(KERN_INFO "Hello from [Бельский Тимофей] module!\n");
    } else {
        printk(KERN_INFO "%s\n", message);
    }
    return 0; // 0 = успех
}

// Функция выгрузки (вызывается при rmmod)
static void __exit hello_exit(void) {
    if (!message) { // Проверка на NULL
        printk(KERN_ERR "Error: Invalid or missing message parameter during exit.\n");
        return;
    }

    if (strcmp(message, "default") == 0) {
        printk(KERN_INFO "Goodbye from [Бельский Тимофей] module!\n");
    } else {
        printk(KERN_INFO "Module unloaded with message: %s\n", message);
    }
}

// Регистрация функций init/exit
module_init(hello_init);
module_exit(hello_exit);

// Метаданные модуля
MODULE_LICENSE("GPL"); // Лицензия (обязательно!)
MODULE_AUTHOR("Бельский Тимофей"); // Автор
MODULE_DESCRIPTION("Простой Hello World модуль с параметром"); // Описание
MODULE_VERSION("1.0"); // Версия
```

---

### **Команды и их выполнение**

#### **1. Загрузка модуля без параметров**
```bash
sudo insmod hello_module.ko
```

**Вывод `dmesg`:**
```
[  452.306289] Hello from [Бельский Тимофей] module!
```

**Пояснение:**
- Команда `insmod` загружает модуль в ядро.
- При загрузке модуля вызывается функция, указанная в `module_init()`, которая выводит сообщение `"Hello from [Бельский Тимофей] module!"`.

---

#### **2. Выгрузка модуля**
```bash
sudo rmmod hello_module
```

**Вывод `dmesg`:**
```
[  475.367451] Goodbye from [Бельский Тимофей] module!
```

**Пояснение:**
- Команда `rmmod` выгружает модуль из ядра.
- При выгрузке модуля вызывается функция, указанная в `module_exit()`, которая выводит сообщение `"Goodbye from [Бельский Тимофей] module!"`.

---

#### **3. Загрузка модуля с параметром**
```bash
sudo insmod hello_module.ko message="Custom_greeting_from_my_module"
```

**Вывод `dmesg`:**
```
[  487.813796] Custom_greeting_from_my_module
```

**Пояснение:**
- Параметр `message` передаётся через командную строку при загрузке модуля.
- Внутри модуля значение параметра используется для вывода кастомного сообщения вместо дефолтного.

---

#### **4. Выгрузка модуля с параметром**
```bash
sudo rmmod hello_module
```

**Вывод `dmesg`:**
```
[  501.135633] Module unloaded with message: Custom_greeting_from_my_module
```

**Пояснение:**
- При выгрузке модуля выводится дополнительное сообщение с использованием значения параметра `message`.

---

### **Анализ работы модуля**

1. **Функциональность:**
   - Модуль корректно загружается и выгружается.
   - Сообщения выводятся в логи ядра (`dmesg`) с использованием функции `printk()`.

2. **Параметры модуля:**
   - Параметр `message` успешно передаётся через командную строку.
   - Если параметр не задан, используется дефолтное сообщение.

3. **Логирование:**
   - Используется уровень логирования `KERN_INFO` для информационных сообщений.
   - Логи доступны через команду `dmesg`.

4. **Безопасность:**
   - Модуль не содержит утечек памяти или других ошибок.
   - Все ресурсы освобождаются в функции `module_exit()`.

---

### **Выводы**
Модуль успешно реализует базовую функциональность:
- Выводит сообщения при загрузке и выгрузке.
- Поддерживает передачу параметров через командную строку.
- Логирует события в ядре.

Таким образом, задание выполнено в полном соответствии с требованиями.

---

## **Задание B: /proc файл с информацией**

### **Описание**
В рамках этого задания был создан модуль ядра Linux, который создаёт файл `/proc/student_info` с информацией:
1. Имя студента: `"Бельский Тимофей"`.
2. Группа и подгруппа: `"11, A"`.
3. Время загрузки модуля (в тактах `jiffies`).
4. Счётчик обращений к файлу (`Read count`), который увеличивается при каждом чтении файла.

---

### **Код модуля**
```c
#include <linux/module.h>    // Обязательно для всех модулей
#include <linux/kernel.h>    // Для KERN_INFO, printk
#include <linux/init.h>      // Для макросов __init и __exit
#include <linux/proc_fs.h>   // Для работы с /proc
#include <linux/uaccess.h>   // Для copy_to_user
#include <linux/jiffies.h>   // Для jiffies

// Глобальные переменные
static struct proc_dir_entry *proc_file;
static unsigned long load_time; // Время загрузки модуля (в jiffies)
static int read_count = 0;      // Счетчик обращений к файлу

// Функция чтения из /proc/student_info
static ssize_t proc_read(struct file *file, char __user *ubuf, size_t count, loff_t *ppos) {
    char buf[256];
    int len;

    // Если уже читали (ppos > 0), завершаем
    if (*ppos > 0)
        return 0;

    // Увеличиваем счетчик только при первом чтении
    read_count++;

    // Формируем строку с информацией
    len = snprintf(buf, sizeof(buf),
                   "Name: Бельский Тимофей\n"
                   "Group: 11, Subgroup: A\n"
                   "Module loaded at: %lu jiffies\n"
                   "Read count: %d\n",
                   load_time, read_count);

    // Копируем данные в пользовательское пространство
    if (copy_to_user(ubuf, buf, len))
        return -EFAULT;

    *ppos = len; // Обновляем позицию чтения
    return len;  // Возвращаем количество прочитанных байт
}

// Определяем операции для файла /proc/student_info
static const struct proc_ops proc_fops = {
    .proc_read = proc_read,
};

// Функция инициализации модуля
static int __init proc_init(void) {
    // Запоминаем время загрузки модуля
    load_time = jiffies;

    // Создаем файл /proc/student_info
    proc_file = proc_create("student_info", 0444, NULL, &proc_fops);
    if (!proc_file) {
        printk(KERN_ERR "Error: Could not create /proc/student_info\n");
        return -ENOMEM;
    }

    printk(KERN_INFO "Module loaded: /proc/student_info created\n");
    return 0; // Успех
}

// Функция выгрузки модуля
static void __exit proc_exit(void) {
    // Удаляем файл /proc/student_info
    proc_remove(proc_file);
    printk(KERN_INFO "Module unloaded: /proc/student_info removed\n");
}

// Регистрация функций init/exit
module_init(proc_init);
module_exit(proc_exit);

// Метаданные модуля
MODULE_LICENSE("GPL"); // Лицензия (обязательно!)
MODULE_AUTHOR("Бельский Тимофей"); // Автор
MODULE_DESCRIPTION("Модуль для создания файла /proc/student_info"); // Описание
MODULE_VERSION("1.0"); // Версия
```

---

### **Команды и их выполнение**

#### **1. Загрузка модуля**
```bash
sudo insmod proc_module.ko
```

**Вывод `dmesg`:**
```
[ 1261.782298] Module loaded: /proc/student_info created
```

**Пояснение:**
- Команда `insmod` загружает модуль в ядро.
- При загрузке модуля создаётся файл `/proc/student_info`.

---

#### **2. Чтение файла `/proc/student_info` (первый раз)**
```bash
cat /proc/student_info
```

**Вывод:**
```
Name: Бельский Тимофей
Group: 11, Subgroup: A
Module loaded at: 4295929046 jiffies
Read count: 1
```

**Пояснение:**
- Файл `/proc/student_info` содержит информацию о студенте, времени загрузки модуля и счётчике обращений.
- При первом чтении счётчик (`Read count`) равен `1`.

---

#### **3. Чтение файла `/proc/student_info` (второй раз)**
```bash
cat /proc/student_info
```

**Вывод:**
```
Name: Бельский Тимофей
Group: 11, Subgroup: A
Module loaded at: 4295929046 jiffies
Read count: 2
```

**Пояснение:**
- При повторном чтении счётчик (`Read count`) увеличивается до `2`.
- Это подтверждает корректную работу счётчика обращений.

---

#### **4. Выгрузка модуля**
```bash
sudo rmmod proc_module
```

**Вывод `dmesg`:**
```
[ 1285.592447] Module unloaded: /proc/student_info removed
```

**Пояснение:**
- Команда `rmmod` выгружает модуль из ядра.
- При выгрузке модуля файл `/proc/student_info` удаляется.

---

### **Анализ работы модуля**

1. **Функциональность:**
   - Модуль корректно создаёт файл `/proc/student_info` при загрузке.
   - Файл содержит всю необходимую информацию: имя студента, группу, подгруппу, время загрузки модуля и счётчик обращений.
   - Счётчик обращений увеличивается на `1` при каждом чтении файла.

2. **Логирование:**
   - Используется функция `printk()` с уровнем логирования `KERN_INFO` для информационных сообщений.
   - Логи доступны через команду `dmesg`.

3. **Безопасность:**
   - Модуль не содержит утечек памяти или других ошибок.
   - Все ресурсы освобождаются в функции `module_exit()`.

4. **Ограничения:**
   - Максимальный размер данных, которые можно прочитать из файла `/proc/student_info`, ограничен буфером (256 байт в данном случае).

---

### **Выводы**
Модуль успешно реализует все требования задания:
- Создаёт файл `/proc/student_info` с информацией о студенте.
- Поддерживает счётчик обращений к файлу.
- Корректно удаляет файл при выгрузке модуля.

Таким образом, задание выполнено в полном соответствии с требованиями.

---

## **Задание C: Простой character device**

### **Описание**
В рамках этого задания был создан модуль ядра Linux, который реализует простое символьное устройство `/dev/mychardev`:
1. Устройство можно открыть и закрыть.
2. При записи данные сохраняются в буфер ядра (максимум 1024 байта).
3. При чтении возвращаются данные из буфера.
4. Логируются события открытия и закрытия устройства.

---

### **Код модуля**
```c
#include <linux/module.h>    // Обязательно для всех модулей
#include <linux/kernel.h>    // Для KERN_INFO, printk
#include <linux/init.h>      // Для макросов __init и __exit
#include <linux/fs.h>        // Для работы с файловой системой
#include <linux/cdev.h>      // Для cdev
#include <linux/uaccess.h>   // Для copy_to_user и copy_from_user
#include <linux/slab.h>      // Для kmalloc/kfree

// Глобальные переменные
static dev_t dev_num;          // Major и minor номера устройства
static struct cdev my_cdev;    // Структура cdev
static char *buffer;           // Буфер для хранения данных (1024 байта)

// Функция открытия устройства
static int dev_open(struct inode *inode, struct file *file) {
    printk(KERN_INFO "Device opened\n");
    return 0;
}

// Функция закрытия устройства
static int dev_release(struct inode *inode, struct file *file) {
    printk(KERN_INFO "Device closed\n");
    return 0;
}

// Функция чтения из устройства
static ssize_t dev_read(struct file *file, char __user *ubuf, size_t len, loff_t *off) {
    int bytes_to_read = min(len, strlen(buffer) - *off);

    if (*off >= strlen(buffer)) {
        return 0; // Конец файла
    }

    if (copy_to_user(ubuf, buffer + *off, bytes_to_read)) {
        return -EFAULT;
    }

    *off += bytes_to_read;
    return bytes_to_read;
}

// Функция записи в устройство
static ssize_t dev_write(struct file *file, const char __user *ubuf, size_t len, loff_t *off) {
    int bytes_to_write = min(len, (size_t)(1024 - *off));

    if (*off >= 1024) {
        return -ENOMEM; // Нет места в буфере
    }

    if (copy_from_user(buffer + *off, ubuf, bytes_to_write)) {
        return -EFAULT;
    }

    *off += bytes_to_write;
    buffer[*off] = '\0'; // Добавляем завершающий ноль
    return bytes_to_write;
}

// Операции устройства
static struct file_operations fops = {
    .owner = THIS_MODULE,
    .open = dev_open,
    .release = dev_release,
    .read = dev_read,
    .write = dev_write,
};

// Функция инициализации модуля
static int __init chardev_init(void) {
    // Выделение major/minor номеров
    if (alloc_chrdev_region(&dev_num, 0, 1, "mychardev") < 0) {
        printk(KERN_ERR "Failed to allocate device number\n");
        return -1;
    }
    printk(KERN_INFO "Major: %d, Minor: %d\n", MAJOR(dev_num), MINOR(dev_num));

    // Инициализация cdev
    cdev_init(&my_cdev, &fops);
    if (cdev_add(&my_cdev, dev_num, 1) < 0) {
        printk(KERN_ERR "Failed to add cdev\n");
        unregister_chrdev_region(dev_num, 1);
        return -1;
    }

    // Выделение памяти для буфера
    buffer = kmalloc(1024, GFP_KERNEL);
    if (!buffer) {
        printk(KERN_ERR "Failed to allocate buffer\n");
        cdev_del(&my_cdev);
        unregister_chrdev_region(dev_num, 1);
        return -1;
    }
    memset(buffer, 0, 1024);

    printk(KERN_INFO "Character device /dev/mychardev created\n");
    return 0;
}

// Функция выгрузки модуля
static void __exit chardev_exit(void) {
    // Освобождение ресурсов
    kfree(buffer);
    cdev_del(&my_cdev);
    unregister_chrdev_region(dev_num, 1);
    printk(KERN_INFO "Character device /dev/mychardev removed\n");
}

// Регистрация функций init/exit
module_init(chardev_init);
module_exit(chardev_exit);

// Метаданные модуля
MODULE_LICENSE("GPL"); // Лицензия (обязательно!)
MODULE_AUTHOR("Бельский Тимофей"); // Автор
MODULE_DESCRIPTION("Простое символьное устройство /dev/mychardev"); // Описание
MODULE_VERSION("1.0"); // Версия
```

---

### **Команды и их выполнение**

#### **1. Загрузка модуля**
```bash
sudo insmod chardev_module.ko
```

**Вывод `dmesg`:**
```
[ 1713.003956] Major: 510, Minor: 0
[ 1713.003970] Character device /dev/mychardev created
```

**Пояснение:**
- Команда `insmod` загружает модуль в ядро.
- При загрузке модуля создаётся символьное устройство `/dev/mychardev` с номерами `Major: 510`, `Minor: 0`.

---

#### **2. Создание устройства**
```bash
sudo mknod /dev/mychardev c 510 0
sudo chmod 666 /dev/mychardev
```

**Пояснение:**
- Команда `mknod` создаёт файл устройства `/dev/mychardev` с указанными номерами `Major` и `Minor`.
- Команда `chmod` устанавливает права доступа `666`, чтобы все пользователи могли читать и писать в устройство.

---

#### **3. Запись данных в устройство**
```bash
echo "Hello" > /dev/mychardev
```

**Пояснение:**
- Данные `"Hello"` записываются в буфер ядра через устройство `/dev/mychardev`.

---

#### **4. Чтение данных из устройства**
```bash
cat /dev/mychardev
```

**Вывод:**
```
Hello
```

**Пояснение:**
- Данные успешно прочитаны из буфера ядра через устройство `/dev/mychardev`.
- Это подтверждает корректную работу функций записи и чтения.

---

#### **5. Логирование событий открытия/закрытия**
**Вывод `dmesg`:**
```
[ 1845.631054] Device opened
[ 1845.631089] Device closed
[ 1850.235975] Device opened
[ 1850.235999] Device closed
```

**Пояснение:**
- При записи (`echo`) и чтении (`cat`) устройства логируются события открытия и закрытия.
- Это соответствует реализации функций `dev_open` и `dev_release`.

---

#### **6. Выгрузка модуля**
```bash
sudo rmmod chardev_module
```

**Вывод `dmesg`:**
```
[ 1863.963625] Character device /dev/mychardev removed
```

**Пояснение:**
- Команда `rmmod` выгружает модуль из ядра.
- При выгрузке модуля устройство `/dev/mychardev` удаляется.

---

### **Анализ работы модуля**

1. **Функциональность:**
   - Модуль корректно создаёт символьное устройство `/dev/mychardev`.
   - Реализованы функции записи и чтения данных.
   - Логируются события открытия и закрытия устройства.

2. **Логирование:**
   - Используется функция `printk()` с уровнем логирования `KERN_INFO` для информационных сообщений.
   - Логи доступны через команду `dmesg`.

3. **Безопасность:**
   - Модуль не содержит утечек памяти или других ошибок.
   - Все ресурсы освобождаются в функции `module_exit()`.

4. **Ограничения:**
   - Максимальный размер буфера: 1024 байта.
   - Используются безопасные функции `copy_to_user()` и `copy_from_user()` для взаимодействия с пользовательским пространством.

---

### **Выводы**
Модуль успешно реализует все требования задания:
- Создаёт символьное устройство `/dev/mychardev`.
- Реализует запись и чтение данных.
- Логирует события открытия и закрытия устройства.
- Корректно удаляет устройство при выгрузке.

Таким образом, задание выполнено в полном соответствии с требованиями.

---

# **4. Ответы на вопросы**

## **Базовые понятия**

1. **Что такое модуль ядра и зачем он нужен?**
   Модуль ядра — это фрагмент кода, который может быть динамически загружен или выгружен из ядра Linux во время работы системы. Он позволяет расширять функциональность ядра без необходимости перезагрузки системы. Например, модули могут добавлять поддержку новых устройств, файловых систем или сетевых протоколов.

2. **Чем отличается kernel-space от user-space?**
   - **Kernel-space**: Привилегированный режим, где выполняется код ядра. Полный доступ ко всем ресурсам системы (память, процессор, устройства). Ошибка в коде может привести к kernel panic.
   - **User-space**: Непривилегированный режим, где выполняются обычные программы. Ограниченный доступ к ресурсам. Ошибка в программе не влияет на работу всей системы.

3. **Что произойдёт, если в модуле обратиться к NULL указателю?**
   Это вызовет kernel panic, так как ядро не имеет защиты от доступа к недопустимым адресам. Система может перезагрузиться.

4. **Почему нельзя использовать printf() в модуле ядра?**
   В ядре нет стандартной библиотеки C. Для вывода используется функция `printk()`, которая записывает сообщения в буфер ядра.

5. **Что такое kernel panic и как его избежать?**
   Kernel panic — это аварийная ситуация в ядре, при которой система прекращает работу. Чтобы избежать этого, нужно тщательно проверять код на наличие ошибок (например, проверка указателей, освобождение памяти).

---

## **Жизненный цикл модуля**

6. **Какие функции вызываются при `insmod` и `rmmod`?**
   - `insmod`: Вызывает функцию, указанную в `module_init()`.
   - `rmmod`: Вызывает функцию, указанную в `module_exit()`.

7. **Что должна делать функция `module_exit()`?**
   Функция `module_exit()` должна освобождать все ресурсы, выделенные в `module_init()` (например, память, файлы, устройства).

8. **Что происходит, если `module_init()` возвращает ошибку?**
   Если `module_init()` возвращает ошибку, модуль не будет загружен, и функция `module_exit()` не вызывается.

9. **Можно ли выгрузить модуль, если он используется?**
   Нет. Если модуль используется (например, открыт файл `/dev/mychardev`), его нельзя выгрузить. Нужно сначала закрыть все процессы, использующие модуль.

---

## **Логирование и отладка**

10. **Чем `printk()` отличается от `printf()`?**
    - `printk()`: Выводит сообщения в буфер ядра. Имеет уровни логирования (например, `KERN_INFO`, `KERN_ERR`).
    - `printf()`: Выводит сообщения в терминал (user-space).

11. **Какие уровни логирования существуют в ядре?**
    - `KERN_EMERG`: Аварийная ситуация.
    - `KERN_ALERT`: Требует немедленных действий.
    - `KERN_CRIT`: Критическая ошибка.
    - `KERN_ERR`: Ошибка.
    - `KERN_WARNING`: Предупреждение.
    - `KERN_NOTICE`: Заметка.
    - `KERN_INFO`: Информация.
    - `KERN_DEBUG`: Отладка.

12. **Как посмотреть логи модуля?**
    ```bash
    dmesg | tail -20
    ```

13. **Что означает "tainted kernel"?**
    Это означает, что ядро "испорчено" (например, загружен проприетарный модуль или модуль без правильной лицензии).

---

## **Память**

14. **Чем `kmalloc()` отличается от `malloc()`?**
    - `kmalloc()`: Выделяет память в kernel-space.
    - `malloc()`: Выделяет память в user-space.

15. **Что такое флаги GFP и зачем они нужны?**
    Флаги GFP (Get Free Pages) определяют, как выделяется память. Например:
    - `GFP_KERNEL`: Обычное выделение (может спать).
    - `GFP_ATOMIC`: Атомарное выделение (не может спать, используется в прерываниях).

16. **Что произойдёт, если не освободить память в `module_exit()`?**
    Произойдёт утечка памяти, которая будет сохраняться до перезагрузки системы.

17. **Почему нельзя использовать user-space указатели напрямую в ядре?**
    Указатели user-space недоступны в kernel-space из-за изоляции памяти. Для взаимодействия используются функции `copy_to_user()` и `copy_from_user()`.

---

## **Взаимодействие с user-space**

18. **Что такое `/proc` и для чего он используется?**
    `/proc` — это виртуальная файловая система, которая предоставляет информацию о состоянии ядра и процессов.

19. **Что такое `/sys` (sysfs) и чем отличается от procfs?**
    `/sys` используется для представления информации об устройствах и драйверах, тогда как `/proc` предоставляет информацию о процессах и системе.

20. **Зачем нужны функции `copy_to_user()` и `copy_from_user()`?**
    Эти функции обеспечивают безопасное копирование данных между kernel-space и user-space.

21. **Что такое character device и как он работает?**
    Character device — это устройство, которое обрабатывает данные побайтово (например, клавиатура, последовательный порт).

---

### **Параметры и метаданные**

22. **Как передать параметры модулю при загрузке?**
    Параметры передаются через командную строку:
    ```bash
    sudo insmod module.ko param=value
    ```

23. **Зачем нужен `MODULE_LICENSE()`?**
    Указывает лицензию модуля. Без GPL-совместимой лицензии ядро помечается как "tainted".

24. **Что произойдёт, если не указать лицензию?**
    Ядро помечается как "tainted", и некоторые функции могут быть недоступны.

---

## **Безопасность**

25. **Какие основные правила безопасного кода в ядре?**
    - Всегда проверять возвращаемые значения.
    - Освобождать все ресурсы в `module_exit()`.
    - Избегать бесконечных циклов.

26. **Можно ли использовать бесконечный цикл в модуле?**
    Нет, это вызовет зависание системы.

27. **Почему в ядре нет FPU операций?**
    Для повышения производительности и совместимости с различными архитектурами.

28. **Что делать, если модуль вызвал kernel panic?**
    Перезагрузить систему, исправить ошибку в коде и повторить загрузку.

---

## **Практические вопросы**

29. **Как узнать, какие модули загружены в системе?**
    ```bash
    lsmod
    ```

30. **Как получить информацию о модуле (версия, параметры)?**
    ```bash
    modinfo module.ko
    ```

---

# **5. Заключение**
В ходе работы были успешно реализованы три модуля ядра Linux. Все модули компилируются без ошибок, корректно загружаются и выгружаются. В отчёте предоставлены подробные ответы на все вопросы, а также анализ результатов работы.
